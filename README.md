These are code notes following the Rust Book 2e by Steve Klabnik et. al.

## Usage
After building all binaries with `cargo build`, any of the following binaries can be executed:

1. Guessing Game
```
cargo run --bin guess
```

2. 